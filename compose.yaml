services:
  al-mehdi:
    build: .
    image: al-mehdi:0.2.0
    ports:
      - "127.0.0.1:8080:8080"
    volumes:
      - al_mehdi_data:/data
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL

volumes:
  al_mehdi_data:
